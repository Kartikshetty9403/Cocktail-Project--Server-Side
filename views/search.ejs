<!DOCTYPE html>
<html>
<head>
  <title>Search Results</title>
  <link rel="stylesheet" href="/styles.css">
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f5f5f5;
      padding: 20px;
    }
    h1 {
      text-align: center;
    }
    .cocktail {
      background: #fff;
      padding: 15px;
      margin: 20px auto;
      border-radius: 12px;
      box-shadow: 0 4px 6px rgba(0,0,0,0.1);
      max-width: 600px;
    }
    .cocktail img {
      width: 200px;
      border-radius: 10px;
      margin-bottom: 10px;
    }
    .cocktail h2 {
      margin: 10px 0;
      color: #333;
    }
    .instructions {
      text-align: left;
      margin-top: 10px;
      font-size: 1rem;
      line-height: 1.4;
    }
    a {
      display: block;
      text-align: center;
      margin-top: 20px;
      color: #007bff;
      text-decoration: none;
      font-weight: bold;
    }
  </style>
</head>
<body>
  <h1>🍸 Search Results</h1>

  <% if (cocktails && cocktails.length > 0) { %>
    <% cocktails.forEach(drink => { %>
      <div class="cocktail">
        <h2><%= drink.strDrink %></h2>
        <img src="<%= drink.strDrinkThumb %>" alt="<%= drink.strDrink %>">
        <div class="instructions">
          <strong>Instructions:</strong> <%= drink.strInstructions %>
        </div>
      </div>
    <% }) %>
  <% } else { %>
    <p style="text-align:center;">No cocktails found. Try another name!</p>
  <% } %>

  <a href="/">⬅ Back to Random Cocktail</a>
</body>
</html>
